name: dart_license_plist
version: 1.1.3

description: command-line tool to generate a license plist file for the Dart programming language.
repository: ^3.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  collection: ^1.19.0
  path: ^1.9.0
  yaml: ^3.1.2
  html: ^0.15.4
  http: ^1.2.2
  interact: ^2.2.0
  pub_updater: ^0.4.0
  xml: ^6.5.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.8

platforms:
  ios:
  macos:
